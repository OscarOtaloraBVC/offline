apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - rbac.yaml
  - pvc.yaml
  - backend-deployment.yaml
  - backend-service.yaml
  - frontend-deployment.yaml
  - frontend-service.yaml

# secretGenerator:
# - name: vault-unseal-secrets
#   literals:
#   - jwt-secret="your-secret-key-change-in-production-please"
#   options:
#     disableNameSuffixHash: true

# configMapGenerator:
# - name: vault-unseal-script  # Para compatibilidad con scripts existentes
#   files:
#   - unseal.sh=scripts/unseal.sh
#   options:
#     disableNameSuffixHash: true