# vault-init-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: vault-init
  namespace: vault
  labels:
    app: vault-init
spec:
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: vault-init