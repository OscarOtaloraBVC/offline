apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rbac-manager-pvc
  namespace: rbac-manager
spec:
  accessModes:
    - ReadWriteOnce
  
  storageClassName: local-path
  
  resources:
    requests:
      storage: 4Gi