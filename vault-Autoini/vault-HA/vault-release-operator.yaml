apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vault-secrets-operator
  namespace: vault
spec:
  interval: 5m
  releaseName: vault-secrets-operator
  chart:
    spec:
      chart: vault-secrets-operator
      version: 0.10.0 #0.9.1
      sourceRef:
        kind: HelmRepository
        name: hashicorp
        namespace: vault
  # values:
  #   controller:
  #     nodeSelector:
  #       nuam/application: vault
  #     tolerations: []